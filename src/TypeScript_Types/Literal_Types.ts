//2. Literal Types (Kiểu dữ liệu cố định):
//string liternal type
let direction: "left" | "right" | "up" | "down";
direction = "left"; // Hợp lệ
direction = "up"; // Hợp lệ
// direction = "forward"; // Lỗi


//Number Literal
let statusCode: 200 | 404 | 500;
statusCode = 200; // Hợp lệ
// statusCode = 300; // Lỗi

